window.CHAPTERS = window.CHAPTERS || [];
window.CHAPTERS.push({
    id: 'ch16',
    number: 16,
    title: 'Galois Theory',
    subtitle: 'Galois group, fixed fields, fundamental theorem of Galois theory',
    sections: [
        // ===== SECTION 1: Galois Groups =====
        {
            id: 'ch16-sec01',
            title: 'Galois Groups',
            content: `
<h2>1 &middot; Galois Groups</h2>

<div class="env-block intuition">
<div class="env-label">The Central Idea</div>
The theory of Galois connects two seemingly unrelated mathematical structures: the subgroups of a group of field automorphisms and the intermediate fields of a field extension. This breathtaking correspondence, discovered by Evariste Galois in the early 19th century, transforms questions about solving polynomial equations into questions about group theory. We begin by defining the Galois group of a field extension.
</div>

<div class="env-block definition">
<div class="env-label">Definition &mdash; Field Automorphism</div>
Let \\(K\\) be a field. A <strong>field automorphism</strong> of \\(K\\) is a ring isomorphism \\(\\sigma : K \\to K\\). That is, \\(\\sigma\\) is a bijection satisfying
\\[\\sigma(a + b) = \\sigma(a) + \\sigma(b), \\quad \\sigma(ab) = \\sigma(a)\\sigma(b)\\]
for all \\(a, b \\in K\\), and \\(\\sigma(1) = 1\\).
</div>

<p>
An automorphism that fixes a subfield element-wise is particularly important.
</p>

<div class="env-block definition">
<div class="env-label">Definition &mdash; Automorphism Group \\(\\operatorname{Aut}(K/F)\\)</div>
Let \\(K/F\\) be a field extension. The <strong>automorphism group</strong> of \\(K\\) over \\(F\\) is
\\[\\operatorname{Aut}(K/F) = \\{\\sigma \\in \\operatorname{Aut}(K) \\mid \\sigma(a) = a \\text{ for all } a \\in F\\}.\\]
This is a group under composition of functions.
</div>

<div class="env-block proposition">
<div class="env-label">Proposition &mdash; \\(\\operatorname{Aut}(K/F)\\) is a Group</div>
The set \\(\\operatorname{Aut}(K/F)\\) forms a group under composition:
<ul>
<li><strong>Closure:</strong> If \\(\\sigma, \\tau\\) fix \\(F\\), then \\(\\sigma \\circ \\tau\\) fixes \\(F\\).</li>
<li><strong>Identity:</strong> The identity map \\(\\operatorname{id}_K\\) fixes \\(F\\).</li>
<li><strong>Inverses:</strong> If \\(\\sigma\\) fixes \\(F\\), so does \\(\\sigma^{-1}\\).</li>
<li><strong>Associativity:</strong> Composition of functions is always associative.</li>
</ul>
</div>

<div class="env-block definition">
<div class="env-label">Definition &mdash; Galois Group</div>
When \\(K/F\\) is a <strong>Galois extension</strong> (normal and separable, as we will define in Section 3), we call \\(\\operatorname{Aut}(K/F)\\) the <strong>Galois group</strong> of the extension, written \\(\\operatorname{Gal}(K/F)\\).
</div>

<div class="env-block example">
<div class="env-label">Example &mdash; \\(\\operatorname{Gal}(\\mathbb{Q}(\\sqrt{2})/\\mathbb{Q})\\)</div>
Consider \\(K = \\mathbb{Q}(\\sqrt{2})\\). Any \\(\\sigma \\in \\operatorname{Aut}(K/\\mathbb{Q})\\) is determined by \\(\\sigma(\\sqrt{2})\\). Since \\(\\sigma\\) must preserve the relation \\((\\sqrt{2})^2 = 2\\), we need \\(\\sigma(\\sqrt{2})^2 = 2\\), so \\(\\sigma(\\sqrt{2}) = \\pm\\sqrt{2}\\). Thus \\(\\operatorname{Gal}(\\mathbb{Q}(\\sqrt{2})/\\mathbb{Q}) = \\{\\operatorname{id}, \\sigma\\}\\) where \\(\\sigma(\\sqrt{2}) = -\\sqrt{2}\\). This group is isomorphic to \\(\\mathbb{Z}/2\\mathbb{Z}\\).
</div>

<div class="env-block example">
<div class="env-label">Example &mdash; \\(\\operatorname{Gal}(\\mathbb{Q}(\\sqrt{2}, \\sqrt{3})/\\mathbb{Q})\\)</div>
Here \\(K = \\mathbb{Q}(\\sqrt{2}, \\sqrt{3})\\) has degree 4 over \\(\\mathbb{Q}\\). An automorphism is determined by its action on \\(\\sqrt{2}\\) and \\(\\sqrt{3}\\) independently: each can be sent to its negative. The four automorphisms are:
\\[\\operatorname{id},\\quad \\sigma_1: \\sqrt{2} \\mapsto -\\sqrt{2},\\quad \\sigma_2: \\sqrt{3} \\mapsto -\\sqrt{3},\\quad \\sigma_3: \\sqrt{2} \\mapsto -\\sqrt{2},\\, \\sqrt{3} \\mapsto -\\sqrt{3}.\\]
This gives \\(\\operatorname{Gal}(K/\\mathbb{Q}) \\cong \\mathbb{Z}/2\\mathbb{Z} \\times \\mathbb{Z}/2\\mathbb{Z}\\), the Klein four-group.
</div>

<div class="env-block example">
<div class="env-label">Example &mdash; \\(\\operatorname{Gal}(\\mathbb{F}_{p^n}/\\mathbb{F}_p)\\)</div>
For finite fields, the Galois group is cyclic, generated by the <strong>Frobenius automorphism</strong> \\(\\varphi: x \\mapsto x^p\\). We have
\\[\\operatorname{Gal}(\\mathbb{F}_{p^n}/\\mathbb{F}_p) = \\langle \\varphi \\rangle \\cong \\mathbb{Z}/n\\mathbb{Z}.\\]
The key fact is that \\(\\varphi^n = \\operatorname{id}\\) on \\(\\mathbb{F}_{p^n}\\) (since \\(x^{p^n} = x\\) for all elements) but \\(\\varphi^k \\neq \\operatorname{id}\\) for \\(0 < k < n\\).
</div>

<div class="env-block theorem">
<div class="env-label">Theorem &mdash; Bound on \\(|\\operatorname{Aut}(K/F)|\\)</div>
If \\(K/F\\) is a finite extension of degree \\(n = [K:F]\\), then
\\[|\\operatorname{Aut}(K/F)| \\le [K:F].\\]
Equality holds if and only if \\(K/F\\) is a Galois extension (as we will see in Section 3).
</div>

<div class="env-block warning">
<div class="env-label">Warning &mdash; Not Every Extension is Galois</div>
Consider \\(\\mathbb{Q}(\\sqrt[3]{2})/\\mathbb{Q}\\). The minimal polynomial of \\(\\sqrt[3]{2}\\) is \\(x^3 - 2\\), which has roots \\(\\sqrt[3]{2},\\, \\omega\\sqrt[3]{2},\\, \\omega^2\\sqrt[3]{2}\\) where \\(\\omega = e^{2\\pi i/3}\\). Since \\(\\omega\\sqrt[3]{2} \\notin \\mathbb{Q}(\\sqrt[3]{2}) \\subset \\mathbb{R}\\), there is no automorphism sending \\(\\sqrt[3]{2}\\) to \\(\\omega\\sqrt[3]{2}\\). Thus \\(|\\operatorname{Aut}(\\mathbb{Q}(\\sqrt[3]{2})/\\mathbb{Q})| = 1 < 3 = [\\mathbb{Q}(\\sqrt[3]{2}):\\mathbb{Q}]\\).
</div>

<div class="viz-container" data-viz="galois-root-permutation">
<div class="viz-canvas" id="galois-root-permutation"></div>
<div class="viz-controls" id="galois-root-permutation-controls"></div>
<div class="viz-caption">Visualize how Galois group elements permute the roots of a polynomial. Click the automorphism buttons to see which roots are swapped.</div>
</div>
`,
            visualizations: [
                {
                    id: 'galois-root-permutation',
                    setup: function(container, controls) {
                        var viz = new VizEngine(container, { scale: 60 });
                        var roots = [
                            { label: '\\u221a2', x: 1.414, y: 0, color: viz.colors.blue },
                            { label: '-\\u221a2', x: -1.414, y: 0, color: viz.colors.teal },
                            { label: '\\u221a3', x: 1.732, y: 1.5, color: viz.colors.orange },
                            { label: '-\\u221a3', x: -1.732, y: 1.5, color: viz.colors.red }
                        ];
                        var currentPerm = [0, 1, 2, 3];
                        var animProgress = 1;
                        var prevPositions = null;
                        var targetPositions = null;

                        var perms = [
                            { name: 'id', perm: [0, 1, 2, 3], desc: 'Identity' },
                            { name: '\\u03c3\\u2081', perm: [1, 0, 2, 3], desc: '\\u221a2 \\u2194 -\\u221a2' },
                            { name: '\\u03c3\\u2082', perm: [0, 1, 3, 2], desc: '\\u221a3 \\u2194 -\\u221a3' },
                            { name: '\\u03c3\\u2083', perm: [1, 0, 3, 2], desc: 'Both swapped' }
                        ];

                        controls.innerHTML = '<div style="display:flex;gap:8px;flex-wrap:wrap;justify-content:center;">' +
                            perms.map(function(p, i) {
                                return '<button class="viz-btn" data-idx="' + i + '">' + p.name + ': ' + p.desc + '</button>';
                            }).join('') + '</div>';

                        controls.querySelectorAll('.viz-btn').forEach(function(btn) {
                            btn.addEventListener('click', function() {
                                var idx = parseInt(this.getAttribute('data-idx'));
                                prevPositions = roots.map(function(r, i) {
                                    return { x: roots[currentPerm[i]].x, y: roots[currentPerm[i]].y };
                                });
                                currentPerm = perms[idx].perm;
                                targetPositions = roots.map(function(r, i) {
                                    return { x: roots[currentPerm[i]].x, y: roots[currentPerm[i]].y };
                                });
                                animProgress = 0;
                            });
                        });

                        function lerp(a, b, t) { return a + (b - a) * t; }

                        function draw() {
                            viz.clear();
                            var ctx = viz.ctx;

                            ctx.fillStyle = viz.colors.text;
                            ctx.font = 'bold 16px -apple-system,sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('Gal(Q(\\u221a2,\\u221a3)/Q) \\u2245 V\\u2084 (Klein four-group)', viz.width / 2, 30);

                            ctx.strokeStyle = viz.colors.grid;
                            ctx.lineWidth = 1;
                            ctx.beginPath();
                            ctx.arc(viz.originX, viz.originY, 2 * viz.scale, 0, 2 * Math.PI);
                            ctx.stroke();

                            if (animProgress < 1) {
                                animProgress = Math.min(1, animProgress + 0.03);
                            }
                            var t = animProgress < 1 ? (1 - Math.cos(animProgress * Math.PI)) / 2 : 1;

                            for (var i = 0; i < roots.length; i++) {
                                var target = roots[currentPerm[i]];
                                var dx, dy;
                                if (prevPositions && animProgress < 1) {
                                    dx = lerp(prevPositions[i].x, target.x, t);
                                    dy = lerp(prevPositions[i].y, target.y, t);
                                } else {
                                    dx = target.x;
                                    dy = target.y;
                                }

                                var sx = viz.originX + dx * viz.scale;
                                var sy = viz.originY - dy * viz.scale;

                                ctx.fillStyle = roots[i].color;
                                ctx.beginPath();
                                ctx.arc(sx, sy, 10, 0, 2 * Math.PI);
                                ctx.fill();

                                ctx.fillStyle = '#fff';
                                ctx.font = '12px -apple-system,sans-serif';
                                ctx.textAlign = 'center';
                                ctx.textBaseline = 'bottom';
                                ctx.fillText(roots[i].label, sx, sy - 14);
                            }

                            if (animProgress < 1) {
                                requestAnimationFrame(draw);
                            }
                        }

                        draw();
                        controls.querySelectorAll('.viz-btn').forEach(function(btn) {
                            btn.addEventListener('click', function() { draw(); });
                        });
                    }
                }
            ],
            exercises: [
                {
                    id: 'ch16-ex01',
                    type: 'short-answer',
                    question: 'Compute the Galois group Gal(Q(i)/Q) and identify the group up to isomorphism.',
                    hint: 'What are the possible images of i under a Q-automorphism?',
                    solution: 'Since i satisfies x^2 + 1 = 0, any Q-automorphism must send i to a root of x^2 + 1, namely i or -i. So Gal(Q(i)/Q) = {id, sigma} where sigma(i) = -i (complex conjugation restricted to Q(i)). This group is isomorphic to Z/2Z.'
                },
                {
                    id: 'ch16-ex02',
                    type: 'short-answer',
                    question: 'Let K = Q(sqrt(2), sqrt(3), sqrt(5)). What is |Gal(K/Q)|? Identify the group.',
                    hint: 'Each square root can be independently negated.',
                    solution: 'The extension has degree 8 over Q, and each of the three square roots can be independently sent to its negative, giving 2^3 = 8 automorphisms. So Gal(K/Q) is isomorphic to (Z/2Z)^3.'
                },
                {
                    id: 'ch16-ex03',
                    type: 'short-answer',
                    question: 'Show that |Aut(Q(cbrt(2))/Q)| = 1, where cbrt(2) denotes the real cube root of 2.',
                    hint: 'Where can an automorphism send cbrt(2)? Remember Q(cbrt(2)) is a subfield of R.',
                    solution: 'An automorphism sigma must send cbrt(2) to a root of x^3 - 2. The roots are cbrt(2), omega*cbrt(2), omega^2*cbrt(2) where omega = e^(2pi*i/3). Since Q(cbrt(2)) is contained in R, sigma(cbrt(2)) must be real, so sigma(cbrt(2)) = cbrt(2). Thus sigma = id and |Aut(Q(cbrt(2))/Q)| = 1.'
                }
            ]
        },

        // ===== SECTION 2: Fixed Fields =====
        {
            id: 'ch16-sec02',
            title: 'Fixed Fields',
            content: `
<h2>2 &middot; Fixed Fields</h2>

<div class="env-block intuition">
<div class="env-label">From Groups Back to Fields</div>
In Section 1, we went from a field extension \\(K/F\\) to a group \\(\\operatorname{Aut}(K/F)\\). Now we reverse the direction: given a group of automorphisms \\(H \\le \\operatorname{Aut}(K)\\), we construct the subfield of elements fixed by every automorphism in \\(H\\). This two-way construction is the engine of the Galois correspondence.
</div>

<div class="env-block definition">
<div class="env-label">Definition &mdash; Fixed Field</div>
Let \\(K\\) be a field and \\(H \\le \\operatorname{Aut}(K)\\) a subgroup. The <strong>fixed field</strong> of \\(H\\) is
\\[K^H = \\{a \\in K \\mid \\sigma(a) = a \\text{ for all } \\sigma \\in H\\}.\\]
It is straightforward to verify that \\(K^H\\) is indeed a subfield of \\(K\\).
</div>

<div class="env-block proposition">
<div class="env-label">Proposition &mdash; \\(K^H\\) is a Subfield</div>
For any subgroup \\(H \\le \\operatorname{Aut}(K)\\), the fixed field \\(K^H\\) is a subfield of \\(K\\). Indeed:
<ul>
<li>\\(0, 1 \\in K^H\\) since automorphisms fix 0 and 1.</li>
<li>If \\(a, b \\in K^H\\), then \\(\\sigma(a \\pm b) = \\sigma(a) \\pm \\sigma(b) = a \\pm b\\), so \\(a \\pm b \\in K^H\\).</li>
<li>Similarly \\(ab \\in K^H\\) and \\(a^{-1} \\in K^H\\) for \\(a \\neq 0\\).</li>
</ul>
</div>

<div class="env-block example">
<div class="env-label">Example &mdash; Fixed Field of Complex Conjugation</div>
Let \\(K = \\mathbb{C}\\) and \\(H = \\{\\operatorname{id}, \\overline{\\cdot}\\}\\) where \\(\\overline{\\cdot}\\) denotes complex conjugation. Then
\\[\\mathbb{C}^H = \\{z \\in \\mathbb{C} \\mid \\bar{z} = z\\} = \\mathbb{R}.\\]
</div>

<div class="env-block example">
<div class="env-label">Example &mdash; Fixed Fields of \\(\\operatorname{Gal}(\\mathbb{Q}(\\sqrt{2}, \\sqrt{3})/\\mathbb{Q})\\)</div>
Let \\(G = \\operatorname{Gal}(\\mathbb{Q}(\\sqrt{2}, \\sqrt{3})/\\mathbb{Q})\\) with generators \\(\\sigma_1: \\sqrt{2} \\mapsto -\\sqrt{2}\\) and \\(\\sigma_2: \\sqrt{3} \\mapsto -\\sqrt{3}\\). The subgroups and their fixed fields are:
<ul>
<li>\\(\\{e\\} \\to K^{\\{e\\}} = \\mathbb{Q}(\\sqrt{2}, \\sqrt{3})\\)</li>
<li>\\(\\langle \\sigma_1 \\rangle \\to K^{\\langle \\sigma_1 \\rangle} = \\mathbb{Q}(\\sqrt{3})\\)</li>
<li>\\(\\langle \\sigma_2 \\rangle \\to K^{\\langle \\sigma_2 \\rangle} = \\mathbb{Q}(\\sqrt{2})\\)</li>
<li>\\(\\langle \\sigma_1\\sigma_2 \\rangle \\to K^{\\langle \\sigma_1\\sigma_2 \\rangle} = \\mathbb{Q}(\\sqrt{6})\\)</li>
<li>\\(G \\to K^G = \\mathbb{Q}\\)</li>
</ul>
</div>

<div class="env-block theorem">
<div class="env-label">Theorem &mdash; Artin's Theorem</div>
Let \\(G\\) be a finite group of automorphisms of a field \\(K\\), and let \\(F = K^G\\). Then \\(K/F\\) is a finite extension with
\\[[K : F] = |G|.\\]
Moreover, \\(G = \\operatorname{Aut}(K/F)\\).
</div>

<p>
Artin's theorem is a cornerstone: it tells us that any finite group of automorphisms is automatically the full automorphism group of \\(K\\) over the fixed field. This is the key ingredient showing that the Galois correspondence is a bijection.
</p>

<div class="env-block proof">
<div class="env-label">Proof Sketch</div>
The proof uses the <strong>Dedekind independence lemma</strong>: distinct automorphisms \\(\\sigma_1, \\ldots, \\sigma_n\\) of a field \\(K\\) are linearly independent as functions from \\(K\\) to \\(K\\) (viewing \\(K\\) as a vector space over itself). This means no nontrivial \\(K\\)-linear combination \\(\\sum c_i \\sigma_i = 0\\) exists. From this, one shows that \\([K:K^G] \\le |G|\\). The reverse inequality \\([K:K^G] \\ge |G|\\) follows because each element of \\(G\\) gives a distinct \\(K^G\\)-embedding of \\(K\\) into its algebraic closure, and the number of such embeddings is bounded by the degree.
</div>

<div class="env-block theorem">
<div class="env-label">Theorem &mdash; Dedekind Independence Lemma</div>
Let \\(K\\) and \\(L\\) be fields, and let \\(\\sigma_1, \\ldots, \\sigma_n : K \\to L\\) be distinct field homomorphisms. Then \\(\\sigma_1, \\ldots, \\sigma_n\\) are linearly independent over \\(L\\). That is, if \\(c_1, \\ldots, c_n \\in L\\) satisfy
\\[c_1 \\sigma_1(x) + c_2 \\sigma_2(x) + \\cdots + c_n \\sigma_n(x) = 0 \\quad \\text{for all } x \\in K,\\]
then \\(c_1 = c_2 = \\cdots = c_n = 0\\).
</div>

<div class="env-block corollary">
<div class="env-label">Corollary</div>
If \\(K/F\\) is Galois with Galois group \\(G\\), then \\([K:F] = |G|\\) and \\(K^G = F\\).
</div>

<div class="viz-container" data-viz="fixed-field-calculator">
<div class="viz-canvas" id="fixed-field-calculator"></div>
<div class="viz-controls" id="fixed-field-calculator-controls"></div>
<div class="viz-caption">Explore the fixed field construction for \\(\\mathbb{Q}(\\sqrt{2}, \\sqrt{3})/\\mathbb{Q}\\). Select a subgroup \\(H\\) to see which elements of \\(K\\) are fixed by all automorphisms in \\(H\\).</div>
</div>
`,
            visualizations: [
                {
                    id: 'fixed-field-calculator',
                    setup: function(container, controls) {
                        var viz = new VizEngine(container, { scale: 40 });
                        var ctx = viz.ctx;

                        var subgroups = [
                            { name: '{e}', elts: ['id'], fixed: 'Q(\\u221a2, \\u221a3)', degree: 1 },
                            { name: '<\\u03c3\\u2081>', elts: ['id', '\\u03c3\\u2081'], fixed: 'Q(\\u221a3)', degree: 2 },
                            { name: '<\\u03c3\\u2082>', elts: ['id', '\\u03c3\\u2082'], fixed: 'Q(\\u221a2)', degree: 2 },
                            { name: '<\\u03c3\\u2081\\u03c3\\u2082>', elts: ['id', '\\u03c3\\u2081\\u03c3\\u2082'], fixed: 'Q(\\u221a6)', degree: 2 },
                            { name: 'G = V\\u2084', elts: ['id', '\\u03c3\\u2081', '\\u03c3\\u2082', '\\u03c3\\u2081\\u03c3\\u2082'], fixed: 'Q', degree: 4 }
                        ];

                        var selected = 0;

                        controls.innerHTML = '<div style="display:flex;gap:6px;flex-wrap:wrap;justify-content:center;">' +
                            subgroups.map(function(sg, i) {
                                return '<button class="viz-btn" data-idx="' + i + '">H = ' + sg.name + '</button>';
                            }).join('') + '</div>';

                        var elements = [
                            { label: 'a + b\\u221a2 + c\\u221a3 + d\\u221a6', vals: { a: 1, b: 1, c: 1, d: 1 } }
                        ];

                        function draw() {
                            viz.clear();
                            var sg = subgroups[selected];

                            ctx.fillStyle = viz.colors.white;
                            ctx.font = 'bold 18px -apple-system,sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('Fixed Field Calculator', viz.width / 2, 35);

                            ctx.font = '14px -apple-system,sans-serif';
                            ctx.fillStyle = viz.colors.teal;
                            ctx.fillText('Subgroup H = ' + sg.name, viz.width / 2, 65);
                            ctx.fillStyle = viz.colors.orange;
                            ctx.fillText('Elements of H: { ' + sg.elts.join(', ') + ' }', viz.width / 2, 90);

                            var testElt = '1 + \\u221a2 + \\u221a3 + \\u221a6';
                            ctx.fillStyle = viz.colors.white;
                            ctx.font = '13px -apple-system,sans-serif';
                            ctx.fillText('Test element: ' + testElt, viz.width / 2, 125);

                            var y0 = 155;
                            var actions = [];
                            if (sg.elts.indexOf('\\u03c3\\u2081') >= 0) {
                                actions.push({ name: '\\u03c3\\u2081', result: '1 - \\u221a2 + \\u221a3 - \\u221a6', desc: '\\u221a2 \\u2192 -\\u221a2' });
                            }
                            if (sg.elts.indexOf('\\u03c3\\u2082') >= 0) {
                                actions.push({ name: '\\u03c3\\u2082', result: '1 + \\u221a2 - \\u221a3 - \\u221a6', desc: '\\u221a3 \\u2192 -\\u221a3' });
                            }
                            if (sg.elts.indexOf('\\u03c3\\u2081\\u03c3\\u2082') >= 0) {
                                actions.push({ name: '\\u03c3\\u2081\\u03c3\\u2082', result: '1 - \\u221a2 - \\u221a3 + \\u221a6', desc: 'both negated' });
                            }

                            ctx.font = '12px -apple-system,sans-serif';
                            ctx.textAlign = 'left';
                            var leftX = 40;
                            for (var i = 0; i < actions.length; i++) {
                                ctx.fillStyle = viz.colors.purple;
                                ctx.fillText(actions[i].name + '(' + testElt + ') = ' + actions[i].result, leftX, y0 + i * 25);
                                ctx.fillStyle = viz.colors.text;
                                ctx.fillText('(' + actions[i].desc + ')', leftX + 380, y0 + i * 25);
                            }

                            var fixedY = y0 + actions.length * 25 + 30;
                            ctx.textAlign = 'center';
                            ctx.fillStyle = viz.colors.green;
                            ctx.font = 'bold 16px -apple-system,sans-serif';
                            ctx.fillText('Fixed field K^H = ' + sg.fixed, viz.width / 2, fixedY);

                            ctx.fillStyle = viz.colors.blue;
                            ctx.font = '14px -apple-system,sans-serif';
                            ctx.fillText('[K : K^H] = |H| = ' + sg.elts.length + ',   [K^H : Q] = ' + (4 / sg.elts.length), viz.width / 2, fixedY + 30);

                            var basisY = fixedY + 65;
                            ctx.fillStyle = viz.colors.yellow;
                            ctx.font = '13px -apple-system,sans-serif';
                            if (selected === 0) {
                                ctx.fillText('Basis of K^H over Q: {1, \\u221a2, \\u221a3, \\u221a6}', viz.width / 2, basisY);
                            } else if (selected === 1) {
                                ctx.fillText('Basis of K^H over Q: {1, \\u221a3}', viz.width / 2, basisY);
                            } else if (selected === 2) {
                                ctx.fillText('Basis of K^H over Q: {1, \\u221a2}', viz.width / 2, basisY);
                            } else if (selected === 3) {
                                ctx.fillText('Basis of K^H over Q: {1, \\u221a6}', viz.width / 2, basisY);
                            } else {
                                ctx.fillText('Basis of K^H over Q: {1}', viz.width / 2, basisY);
                            }
                        }

                        draw();

                        controls.querySelectorAll('.viz-btn').forEach(function(btn) {
                            btn.addEventListener('click', function() {
                                selected = parseInt(this.getAttribute('data-idx'));
                                draw();
                            });
                        });
                    }
                }
            ],
            exercises: [
                {
                    id: 'ch16-ex04',
                    type: 'short-answer',
                    question: 'Let K = Q(sqrt(5)) and let sigma be the automorphism sending sqrt(5) to -sqrt(5). Verify directly that K^{<sigma>} = Q by showing that if a + b*sqrt(5) is fixed by sigma, then b = 0.',
                    hint: 'Apply sigma to a + b*sqrt(5) and set the result equal to a + b*sqrt(5).',
                    solution: 'sigma(a + b*sqrt(5)) = a - b*sqrt(5). Setting this equal to a + b*sqrt(5) gives -b*sqrt(5) = b*sqrt(5), hence 2b*sqrt(5) = 0. Since sqrt(5) is nonzero, b = 0. So the fixed elements are exactly {a : a in Q} = Q.'
                },
                {
                    id: 'ch16-ex05',
                    type: 'short-answer',
                    question: 'Use Artin\'s theorem to determine [C : R] without computing a basis, given that Aut(C/R) = {id, complex conjugation}.',
                    hint: 'Artin says [K : K^G] = |G|.',
                    solution: 'Let G = {id, conjugation}. Then |G| = 2 and R = C^G (the fixed field of conjugation is R). By Artin\'s theorem, [C : R] = |G| = 2.'
                },
                {
                    id: 'ch16-ex06',
                    type: 'short-answer',
                    question: 'In the extension Q(sqrt(2), sqrt(3))/Q, find the fixed field of the subgroup H = {id, sigma_1*sigma_2} where sigma_1*sigma_2 negates both sqrt(2) and sqrt(3).',
                    hint: 'Which elements a + b*sqrt(2) + c*sqrt(3) + d*sqrt(6) are preserved when both sqrt(2) and sqrt(3) change sign?',
                    solution: 'sigma_1*sigma_2 sends sqrt(2) to -sqrt(2) and sqrt(3) to -sqrt(3), so sqrt(6) = sqrt(2)*sqrt(3) maps to (-sqrt(2))(-sqrt(3)) = sqrt(6). A general element a + b*sqrt(2) + c*sqrt(3) + d*sqrt(6) is fixed iff b = c = 0. So K^H = {a + d*sqrt(6) : a, d in Q} = Q(sqrt(6)).'
                },
                {
                    id: 'ch16-ex07',
                    type: 'short-answer',
                    question: 'State the Dedekind independence lemma and explain why it implies that if G is a finite group of automorphisms of K with |G| = n, then [K : K^G] >= n.',
                    hint: 'Think about what linear independence of automorphisms means for the dimension of K over K^G.',
                    solution: 'Dedekind says distinct field homomorphisms sigma_1,...,sigma_n : K -> L are L-linearly independent. If [K:K^G] = m < n, choose a basis e_1,...,e_m for K over K^G. Define vectors v_j = (sigma_j(e_1),...,sigma_j(e_m)) in K^m. Since m < n, the n vectors v_1,...,v_n are K-linearly dependent, say sum c_j v_j = 0 with not all c_j zero. Then for every x = sum a_i e_i we get sum c_j sigma_j(x) = sum a_i (sum c_j sigma_j(e_i)) = 0, contradicting Dedekind independence. Hence m >= n.'
                }
            ]
        },

        // ===== SECTION 3: Normal and Separable Extensions =====
        {
            id: 'ch16-sec03',
            title: 'Normal and Separable Extensions',
            content: `
<h2>3 &middot; Normal and Separable Extensions</h2>

<div class="env-block intuition">
<div class="env-label">When Does Galois Theory Apply?</div>
Not every field extension admits a Galois correspondence. In Section 1, we saw that \\(\\mathbb{Q}(\\sqrt[3]{2})/\\mathbb{Q}\\) has too few automorphisms. For the full theory to work, the extension must be both <strong>normal</strong> (all irreducible polynomials that have one root in \\(K\\) split completely in \\(K\\)) and <strong>separable</strong> (no repeated roots). This section defines these notions precisely and proves that Galois = normal + separable.
</div>

<div class="env-block definition">
<div class="env-label">Definition &mdash; Normal Extension</div>
A finite extension \\(K/F\\) is <strong>normal</strong> if every irreducible polynomial in \\(F[x]\\) that has at least one root in \\(K\\) splits completely over \\(K\\). Equivalently, \\(K\\) is the splitting field of some polynomial \\(f \\in F[x]\\).
</div>

<div class="env-block example">
<div class="env-label">Example &mdash; Normal vs. Not Normal</div>
<ul>
<li>\\(\\mathbb{Q}(\\sqrt{2})/\\mathbb{Q}\\) is normal: it is the splitting field of \\(x^2 - 2\\) (both roots \\(\\pm\\sqrt{2}\\) lie in \\(\\mathbb{Q}(\\sqrt{2})\\)).</li>
<li>\\(\\mathbb{Q}(\\sqrt[3]{2})/\\mathbb{Q}\\) is <em>not</em> normal: the polynomial \\(x^3 - 2\\) has one root \\(\\sqrt[3]{2}\\) in the field but the other roots \\(\\omega\\sqrt[3]{2}\\) and \\(\\omega^2\\sqrt[3]{2}\\) are not real, hence not in \\(\\mathbb{Q}(\\sqrt[3]{2}) \\subset \\mathbb{R}\\).</li>
</ul>
</div>

<div class="env-block proposition">
<div class="env-label">Proposition &mdash; Characterizations of Normality</div>
For a finite extension \\(K/F\\), the following are equivalent:
<ol>
<li>\\(K/F\\) is normal.</li>
<li>\\(K\\) is the splitting field of some polynomial in \\(F[x]\\).</li>
<li>For every \\(F\\)-embedding \\(\\sigma: K \\hookrightarrow \\bar{F}\\) (where \\(\\bar{F}\\) is an algebraic closure), we have \\(\\sigma(K) = K\\).</li>
</ol>
</div>

<div class="env-block definition">
<div class="env-label">Definition &mdash; Separable Polynomial</div>
A polynomial \\(f \\in F[x]\\) is <strong>separable</strong> if it has no repeated roots in any extension of \\(F\\). Equivalently, \\(\\gcd(f, f') = 1\\) where \\(f'\\) is the formal derivative.
</div>

<div class="env-block definition">
<div class="env-label">Definition &mdash; Separable Extension</div>
An algebraic extension \\(K/F\\) is <strong>separable</strong> if the minimal polynomial of every element \\(\\alpha \\in K\\) over \\(F\\) is separable.
</div>

<div class="env-block proposition">
<div class="env-label">Proposition &mdash; Characteristic Zero is Always Separable</div>
If \\(\\operatorname{char}(F) = 0\\), then every algebraic extension of \\(F\\) is separable. The reason: if \\(f \\in F[x]\\) is irreducible, then \\(f' \\neq 0\\) (since \\(\\deg f' = \\deg f - 1 \\ge 0\\) and leading coefficients are nonzero in characteristic 0), so \\(\\gcd(f, f') = 1\\).
</div>

<div class="env-block warning">
<div class="env-label">Warning &mdash; Inseparability in Positive Characteristic</div>
Over fields of characteristic \\(p > 0\\), inseparable extensions exist. For example, let \\(F = \\mathbb{F}_p(t)\\) and consider \\(\\alpha = t^{1/p}\\). The minimal polynomial is \\(x^p - t\\), which factors as \\((x - \\alpha)^p\\) in \\(F(\\alpha)[x]\\). This polynomial has \\(\\alpha\\) as a root of multiplicity \\(p\\).
</div>

<div class="env-block definition">
<div class="env-label">Definition &mdash; Galois Extension</div>
A finite extension \\(K/F\\) is a <strong>Galois extension</strong> if it is both normal and separable.
</div>

<div class="env-block theorem">
<div class="env-label">Theorem &mdash; Galois = Normal + Separable</div>
For a finite extension \\(K/F\\), the following are equivalent:
<ol>
<li>\\(K/F\\) is Galois (normal and separable).</li>
<li>\\(|\\operatorname{Aut}(K/F)| = [K:F]\\).</li>
<li>\\(K\\) is the splitting field of a separable polynomial in \\(F[x]\\).</li>
<li>\\(K^{\\operatorname{Aut}(K/F)} = F\\) (the fixed field of all automorphisms is exactly \\(F\\)).</li>
</ol>
</div>

<div class="env-block proof">
<div class="env-label">Proof Sketch of Key Direction (1 implies 2)</div>
Suppose \\(K/F\\) is normal and separable, with \\([K:F] = n\\). Since \\(K\\) is separable, the primitive element theorem gives \\(K = F(\\alpha)\\) for some \\(\\alpha\\). Let \\(f = \\min_F(\\alpha)\\), which has degree \\(n\\). Since \\(K\\) is normal, \\(f\\) splits completely in \\(K\\): \\(f(x) = (x - \\alpha_1)\\cdots(x - \\alpha_n)\\) with all \\(\\alpha_i \\in K\\). Since \\(f\\) is separable, the \\(\\alpha_i\\) are distinct. Each \\(\\alpha_i\\) determines an automorphism \\(\\sigma_i \\in \\operatorname{Aut}(K/F)\\) via \\(\\sigma_i(\\alpha) = \\alpha_i\\). So \\(|\\operatorname{Aut}(K/F)| \\ge n\\). Combined with the bound \\(|\\operatorname{Aut}(K/F)| \\le [K:F] = n\\), we get equality.
</div>

<div class="env-block example">
<div class="env-label">Example &mdash; Splitting Field of \\(x^3 - 2\\)</div>
The splitting field of \\(x^3 - 2\\) over \\(\\mathbb{Q}\\) is \\(K = \\mathbb{Q}(\\sqrt[3]{2}, \\omega)\\) where \\(\\omega = e^{2\\pi i/3}\\). This extension has degree 6 over \\(\\mathbb{Q}\\). Since it is a splitting field of a separable polynomial (characteristic 0), it is Galois. The Galois group has order 6, and it acts by permuting the three roots \\(\\sqrt[3]{2},\\, \\omega\\sqrt[3]{2},\\, \\omega^2\\sqrt[3]{2}\\). This gives an embedding into \\(S_3\\), and since \\(|S_3| = 6 = |\\operatorname{Gal}(K/\\mathbb{Q})|\\), we have \\(\\operatorname{Gal}(K/\\mathbb{Q}) \\cong S_3\\).
</div>

<div class="viz-container" data-viz="normal-separable-checker">
<div class="viz-canvas" id="normal-separable-checker"></div>
<div class="viz-controls" id="normal-separable-checker-controls"></div>
<div class="viz-caption">Explore examples of field extensions and check whether they are normal, separable, or Galois.</div>
</div>
`,
            visualizations: [
                {
                    id: 'normal-separable-checker',
                    setup: function(container, controls) {
                        var viz = new VizEngine(container, { scale: 40 });
                        var ctx = viz.ctx;

                        var examples = [
                            {
                                name: 'Q(\\u221a2)/Q',
                                poly: 'x\\u00b2 - 2',
                                roots: ['\\u221a2', '-\\u221a2'],
                                rootsInK: [true, true],
                                normal: true,
                                separable: true,
                                galois: true,
                                galGrp: 'Z/2Z',
                                degree: 2
                            },
                            {
                                name: 'Q(\\u221b2)/Q',
                                poly: 'x\\u00b3 - 2',
                                roots: ['\\u221b2', '\\u03c9\\u221b2', '\\u03c9\\u00b2\\u221b2'],
                                rootsInK: [true, false, false],
                                normal: false,
                                separable: true,
                                galois: false,
                                galGrp: 'trivial (only id)',
                                degree: 3
                            },
                            {
                                name: 'Q(\\u221b2,\\u03c9)/Q',
                                poly: 'x\\u00b3 - 2',
                                roots: ['\\u221b2', '\\u03c9\\u221b2', '\\u03c9\\u00b2\\u221b2'],
                                rootsInK: [true, true, true],
                                normal: true,
                                separable: true,
                                galois: true,
                                galGrp: 'S\\u2083',
                                degree: 6
                            },
                            {
                                name: 'F\\u209a(t^{1/p})/F\\u209a(t)',
                                poly: 'x^p - t',
                                roots: ['t^{1/p} (mult. p)'],
                                rootsInK: [true],
                                normal: true,
                                separable: false,
                                galois: false,
                                galGrp: 'trivial',
                                degree: 'p'
                            }
                        ];

                        var selected = 0;

                        controls.innerHTML = '<div style="display:flex;gap:6px;flex-wrap:wrap;justify-content:center;">' +
                            examples.map(function(ex, i) {
                                return '<button class="viz-btn" data-idx="' + i + '">' + ex.name + '</button>';
                            }).join('') + '</div>';

                        function draw() {
                            viz.clear();
                            var ex = examples[selected];

                            ctx.fillStyle = viz.colors.white;
                            ctx.font = 'bold 18px -apple-system,sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('Extension: ' + ex.name, viz.width / 2, 35);

                            ctx.font = '14px -apple-system,sans-serif';
                            ctx.fillStyle = viz.colors.text;
                            ctx.fillText('Minimal polynomial: ' + ex.poly + '    Degree: [K:F] = ' + ex.degree, viz.width / 2, 65);

                            var rootY = 100;
                            ctx.font = '13px -apple-system,sans-serif';
                            ctx.fillStyle = viz.colors.blue;
                            ctx.fillText('Roots of ' + ex.poly + ':', viz.width / 2, rootY);

                            for (var i = 0; i < ex.roots.length; i++) {
                                var inK = ex.rootsInK[i];
                                ctx.fillStyle = inK ? viz.colors.green : viz.colors.red;
                                var marker = inK ? '  [in K]' : '  [NOT in K]';
                                ctx.fillText(ex.roots[i] + marker, viz.width / 2, rootY + 25 + i * 22);
                            }

                            var checkY = rootY + 25 + ex.roots.length * 22 + 30;

                            var checks = [
                                { label: 'Normal', val: ex.normal },
                                { label: 'Separable', val: ex.separable },
                                { label: 'Galois', val: ex.galois }
                            ];

                            var boxW = 140, boxH = 50, gap = 30;
                            var totalW = checks.length * boxW + (checks.length - 1) * gap;
                            var startX = (viz.width - totalW) / 2;

                            for (var j = 0; j < checks.length; j++) {
                                var bx = startX + j * (boxW + gap);
                                ctx.fillStyle = checks[j].val ? 'rgba(63,185,80,0.2)' : 'rgba(248,81,73,0.2)';
                                ctx.strokeStyle = checks[j].val ? viz.colors.green : viz.colors.red;
                                ctx.lineWidth = 2;
                                ctx.beginPath();
                                ctx.roundRect(bx, checkY, boxW, boxH, 8);
                                ctx.fill();
                                ctx.stroke();

                                ctx.fillStyle = checks[j].val ? viz.colors.green : viz.colors.red;
                                ctx.font = 'bold 14px -apple-system,sans-serif';
                                ctx.textAlign = 'center';
                                ctx.fillText(checks[j].label, bx + boxW / 2, checkY + 22);
                                ctx.font = '12px -apple-system,sans-serif';
                                ctx.fillText(checks[j].val ? 'Yes' : 'No', bx + boxW / 2, checkY + 40);
                            }

                            ctx.fillStyle = viz.colors.orange;
                            ctx.font = '14px -apple-system,sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('Galois group: Gal(K/F) \\u2245 ' + ex.galGrp, viz.width / 2, checkY + boxH + 35);
                        }

                        draw();

                        controls.querySelectorAll('.viz-btn').forEach(function(btn) {
                            btn.addEventListener('click', function() {
                                selected = parseInt(this.getAttribute('data-idx'));
                                draw();
                            });
                        });
                    }
                }
            ],
            exercises: [
                {
                    id: 'ch16-ex08',
                    type: 'short-answer',
                    question: 'Is Q(sqrt(2), sqrt(3))/Q a normal extension? Justify your answer.',
                    hint: 'Show it is a splitting field of a polynomial.',
                    solution: 'Yes. Q(sqrt(2), sqrt(3)) is the splitting field of (x^2 - 2)(x^2 - 3) over Q. Since it is a splitting field of a polynomial in Q[x], the extension is normal.'
                },
                {
                    id: 'ch16-ex09',
                    type: 'short-answer',
                    question: 'Let K be the splitting field of x^4 - 2 over Q. What is [K:Q]? Is K/Q Galois?',
                    hint: 'The roots are the four fourth roots of 2. You need a primitive 4th root of unity.',
                    solution: 'The roots of x^4 - 2 are 2^(1/4), i*2^(1/4), -2^(1/4), -i*2^(1/4). So K = Q(2^(1/4), i). We have [Q(2^(1/4)):Q] = 4 and [Q(2^(1/4),i):Q(2^(1/4))] = 2 (since i is not real), giving [K:Q] = 8. Since K is the splitting field of a separable polynomial (char 0), K/Q is Galois.'
                },
                {
                    id: 'ch16-ex10',
                    type: 'short-answer',
                    question: 'Give an example of a field extension that is separable but not normal.',
                    hint: 'Think about a simple extension by a root of an irreducible cubic over Q.',
                    solution: 'Q(cbrt(2))/Q is separable (since char(Q) = 0, all extensions of Q are separable) but not normal (x^3 - 2 has a root cbrt(2) in Q(cbrt(2)) but does not split there, since the other roots omega*cbrt(2) and omega^2*cbrt(2) are not real).'
                }
            ]
        },

        // ===== SECTION 4: The Fundamental Theorem of Galois Theory =====
        {
            id: 'ch16-sec04',
            title: 'The Fundamental Theorem of Galois Theory',
            content: `
<h2>4 &middot; The Fundamental Theorem of Galois Theory</h2>

<div class="env-block intuition">
<div class="env-label">The Grand Correspondence</div>
The fundamental theorem of Galois theory is one of the most beautiful results in all of algebra. It establishes a perfect, order-reversing bijection between the subgroups of the Galois group and the intermediate fields of a Galois extension. Larger subgroups correspond to smaller fields, and vice versa. Furthermore, normal subgroups correspond to normal (Galois) sub-extensions. This single theorem transforms intricate field-theoretic questions into manageable group-theoretic ones.
</div>

<div class="env-block theorem">
<div class="env-label">The Fundamental Theorem of Galois Theory</div>
Let \\(K/F\\) be a finite Galois extension with Galois group \\(G = \\operatorname{Gal}(K/F)\\). There is an inclusion-reversing bijection
\\[\\{\\text{subgroups } H \\le G\\} \\longleftrightarrow \\{\\text{intermediate fields } F \\subseteq E \\subseteq K\\}\\]
given by:
<ul>
<li><strong>Subgroup \\(\\to\\) Field:</strong> \\(H \\mapsto K^H = \\{a \\in K \\mid \\sigma(a) = a \\text{ for all } \\sigma \\in H\\}\\).</li>
<li><strong>Field \\(\\to\\) Subgroup:</strong> \\(E \\mapsto \\operatorname{Gal}(K/E) = \\{\\sigma \\in G \\mid \\sigma(a) = a \\text{ for all } a \\in E\\}\\).</li>
</ul>
Moreover, this bijection satisfies:
<ol>
<li><strong>Degree-order relation:</strong> \\([K : K^H] = |H|\\) and \\([K^H : F] = [G : H]\\).</li>
<li><strong>Inclusion reversal:</strong> \\(H_1 \\le H_2\\) if and only if \\(K^{H_1} \\supseteq K^{H_2}\\).</li>
<li><strong>Normality criterion:</strong> \\(H \\trianglelefteq G\\) if and only if \\(K^H / F\\) is a normal (hence Galois) extension, in which case
\\[\\operatorname{Gal}(K^H / F) \\cong G / H.\\]</li>
</ol>
</div>

<div class="env-block proof">
<div class="env-label">Proof Outline</div>
<p>
<strong>Step 1 (The maps are well-defined):</strong> By definition, \\(K^H\\) is a subfield of \\(K\\) containing \\(F\\), and \\(\\operatorname{Gal}(K/E)\\) is a subgroup of \\(G\\).
</p>
<p>
<strong>Step 2 (The maps are inverses):</strong> Artin's theorem gives \\(K^{\\operatorname{Gal}(K/E)} = E\\) for any intermediate field \\(E\\) (since \\(K/E\\) is also Galois). For any subgroup \\(H\\), Artin's theorem applied to the group \\(H\\) acting on \\(K\\) gives \\(\\operatorname{Gal}(K/K^H) = H\\).
</p>
<p>
<strong>Step 3 (Degree-order):</strong> By Artin, \\([K:K^H] = |H|\\). Since \\([K:F] = |G|\\), the tower law gives \\([K^H:F] = |G|/|H| = [G:H]\\).
</p>
<p>
<strong>Step 4 (Normality):</strong> If \\(H \\trianglelefteq G\\), then for any \\(\\sigma \\in G\\) and \\(a \\in K^H\\), and any \\(\\tau \\in H\\), we have \\(\\tau(\\sigma(a)) = \\sigma(\\sigma^{-1}\\tau\\sigma)(a) = \\sigma(a)\\) since \\(\\sigma^{-1}\\tau\\sigma \\in H\\). So \\(\\sigma(K^H) \\subseteq K^H\\), which means \\(K^H/F\\) is normal. The restriction map \\(G \\to \\operatorname{Gal}(K^H/F)\\) is surjective with kernel \\(H\\), giving \\(\\operatorname{Gal}(K^H/F) \\cong G/H\\).
</p>
</div>

<div class="env-block example">
<div class="env-label">Example &mdash; Galois Correspondence for \\(\\mathbb{Q}(\\sqrt{2}, \\sqrt{3})/\\mathbb{Q}\\)</div>
<p>
\\(G = \\operatorname{Gal}(\\mathbb{Q}(\\sqrt{2}, \\sqrt{3})/\\mathbb{Q}) \\cong V_4 = \\{e, \\sigma_1, \\sigma_2, \\sigma_1\\sigma_2\\}\\). The subgroup lattice and corresponding field lattice are:
</p>
<p>
<strong>Subgroup lattice (top = larger):</strong>
</p>
\\[G \\supseteq \\langle \\sigma_1 \\rangle, \\langle \\sigma_2 \\rangle, \\langle \\sigma_1\\sigma_2 \\rangle \\supseteq \\{e\\}\\]
<p>
<strong>Field lattice (top = larger, reversed!):</strong>
</p>
\\[\\mathbb{Q}(\\sqrt{2}, \\sqrt{3}) \\supseteq \\mathbb{Q}(\\sqrt{3}), \\mathbb{Q}(\\sqrt{2}), \\mathbb{Q}(\\sqrt{6}) \\supseteq \\mathbb{Q}\\]
<p>
Note the reversal: the trivial subgroup \\(\\{e\\}\\) corresponds to the whole field \\(K\\), and the full group \\(G\\) corresponds to the base field \\(\\mathbb{Q}\\). Every subgroup of \\(V_4\\) is normal (since \\(V_4\\) is abelian), so every intermediate field is Galois over \\(\\mathbb{Q}\\).
</p>
</div>

<div class="env-block example">
<div class="env-label">Example &mdash; Galois Correspondence for \\(S_3\\)</div>
<p>
Let \\(K\\) be the splitting field of \\(x^3 - 2\\) over \\(\\mathbb{Q}\\), so \\(G = \\operatorname{Gal}(K/\\mathbb{Q}) \\cong S_3\\). The subgroups of \\(S_3\\) are:
</p>
<ul>
<li>\\(S_3\\) itself (order 6) \\(\\longleftrightarrow \\mathbb{Q}\\)</li>
<li>\\(A_3 = \\langle (123) \\rangle\\) (order 3, normal) \\(\\longleftrightarrow \\mathbb{Q}(\\omega)\\) (degree 2 over \\(\\mathbb{Q}\\), Galois)</li>
<li>\\(\\langle (12) \\rangle\\) (order 2, not normal) \\(\\longleftrightarrow \\mathbb{Q}(\\omega^2\\sqrt[3]{2})\\) (degree 3, <em>not</em> Galois over \\(\\mathbb{Q}\\))</li>
<li>\\(\\langle (13) \\rangle\\) (order 2, not normal) \\(\\longleftrightarrow \\mathbb{Q}(\\omega\\sqrt[3]{2})\\) (degree 3, not Galois over \\(\\mathbb{Q}\\))</li>
<li>\\(\\langle (23) \\rangle\\) (order 2, not normal) \\(\\longleftrightarrow \\mathbb{Q}(\\sqrt[3]{2})\\) (degree 3, not Galois over \\(\\mathbb{Q}\\))</li>
<li>\\(\\{e\\}\\) (order 1) \\(\\longleftrightarrow K\\)</li>
</ul>
<p>
Notice: \\(A_3\\) is the only proper normal subgroup of \\(S_3\\), and correspondingly \\(\\mathbb{Q}(\\omega)\\) is the only proper intermediate field that is Galois over \\(\\mathbb{Q}\\).
</p>
</div>

<div class="viz-container" data-viz="galois-correspondence-lattice">
<div class="viz-canvas" id="galois-correspondence-lattice"></div>
<div class="viz-controls" id="galois-correspondence-lattice-controls"></div>
<div class="viz-caption">The Galois correspondence: subgroup lattice on the left, field lattice on the right, connected by the order-reversing bijection. Select an example to see both lattices.</div>
</div>

<div class="viz-container" data-viz="x4-minus-2-lattice">
<div class="viz-canvas" id="x4-minus-2-lattice"></div>
<div class="viz-controls" id="x4-minus-2-lattice-controls"></div>
<div class="viz-caption">Complete Galois correspondence for the splitting field of \\(x^4 - 2\\) over \\(\\mathbb{Q}\\). The Galois group is the dihedral group \\(D_4\\) of order 8.</div>
</div>
`,
            visualizations: [
                {
                    id: 'galois-correspondence-lattice',
                    setup: function(container, controls) {
                        var viz = new VizEngine(container, { scale: 40 });
                        var ctx = viz.ctx;

                        var examples = [
                            {
                                name: 'V\\u2084 : Q(\\u221a2,\\u221a3)/Q',
                                subgroups: [
                                    { label: 'V\\u2084', x: 0.5, y: 0.9, order: 4 },
                                    { label: '<\\u03c3\\u2081>', x: 0.2, y: 0.55, order: 2 },
                                    { label: '<\\u03c3\\u2082>', x: 0.5, y: 0.55, order: 2 },
                                    { label: '<\\u03c3\\u2081\\u03c3\\u2082>', x: 0.8, y: 0.55, order: 2 },
                                    { label: '{e}', x: 0.5, y: 0.2, order: 1 }
                                ],
                                subEdges: [[0,1],[0,2],[0,3],[1,4],[2,4],[3,4]],
                                fields: [
                                    { label: 'Q', x: 0.5, y: 0.9, degree: 1 },
                                    { label: 'Q(\\u221a3)', x: 0.2, y: 0.55, degree: 2 },
                                    { label: 'Q(\\u221a2)', x: 0.5, y: 0.55, degree: 2 },
                                    { label: 'Q(\\u221a6)', x: 0.8, y: 0.55, degree: 2 },
                                    { label: 'Q(\\u221a2,\\u221a3)', x: 0.5, y: 0.2, degree: 4 }
                                ],
                                fieldEdges: [[0,1],[0,2],[0,3],[1,4],[2,4],[3,4]]
                            },
                            {
                                name: 'S\\u2083 : Splitting field of x\\u00b3-2',
                                subgroups: [
                                    { label: 'S\\u2083', x: 0.5, y: 0.92, order: 6 },
                                    { label: 'A\\u2083', x: 0.2, y: 0.62, order: 3 },
                                    { label: '<(12)>', x: 0.4, y: 0.62, order: 2 },
                                    { label: '<(13)>', x: 0.6, y: 0.62, order: 2 },
                                    { label: '<(23)>', x: 0.8, y: 0.62, order: 2 },
                                    { label: '{e}', x: 0.5, y: 0.2, order: 1 }
                                ],
                                subEdges: [[0,1],[0,2],[0,3],[0,4],[1,5],[2,5],[3,5],[4,5]],
                                fields: [
                                    { label: 'Q', x: 0.5, y: 0.92, degree: 1 },
                                    { label: 'Q(\\u03c9)', x: 0.2, y: 0.62, degree: 2 },
                                    { label: 'Q(\\u03c9\\u00b2\\u221b2)', x: 0.4, y: 0.62, degree: 3 },
                                    { label: 'Q(\\u03c9\\u221b2)', x: 0.6, y: 0.62, degree: 3 },
                                    { label: 'Q(\\u221b2)', x: 0.8, y: 0.62, degree: 3 },
                                    { label: 'Q(\\u221b2,\\u03c9)', x: 0.5, y: 0.2, degree: 6 }
                                ],
                                fieldEdges: [[0,1],[0,2],[0,3],[0,4],[1,5],[2,5],[3,5],[4,5]]
                            }
                        ];

                        var selected = 0;

                        controls.innerHTML = '<div style="display:flex;gap:8px;flex-wrap:wrap;justify-content:center;">' +
                            examples.map(function(ex, i) {
                                return '<button class="viz-btn" data-idx="' + i + '">' + ex.name + '</button>';
                            }).join('') + '</div>';

                        function drawLattice(nodes, edges, offsetX, title, color) {
                            var halfW = viz.width * 0.42;
                            var topY = 50;
                            var botY = viz.height - 30;
                            var leftX = offsetX;

                            ctx.fillStyle = color;
                            ctx.font = 'bold 14px -apple-system,sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText(title, leftX + halfW / 2, 25);

                            ctx.strokeStyle = 'rgba(255,255,255,0.2)';
                            ctx.lineWidth = 1.5;
                            for (var e = 0; e < edges.length; e++) {
                                var a = nodes[edges[e][0]];
                                var b = nodes[edges[e][1]];
                                var ax = leftX + a.x * halfW;
                                var ay = topY + a.y * (botY - topY);
                                var bx = leftX + b.x * halfW;
                                var by = topY + b.y * (botY - topY);
                                ctx.beginPath();
                                ctx.moveTo(ax, ay);
                                ctx.lineTo(bx, by);
                                ctx.stroke();
                            }

                            for (var n = 0; n < nodes.length; n++) {
                                var nd = nodes[n];
                                var nx = leftX + nd.x * halfW;
                                var ny = topY + nd.y * (botY - topY);

                                ctx.fillStyle = color;
                                ctx.beginPath();
                                ctx.arc(nx, ny, 6, 0, 2 * Math.PI);
                                ctx.fill();

                                ctx.fillStyle = viz.colors.white;
                                ctx.font = '11px -apple-system,sans-serif';
                                ctx.textAlign = 'center';
                                ctx.textBaseline = 'bottom';
                                ctx.fillText(nd.label, nx, ny - 10);
                            }
                        }

                        function draw() {
                            viz.clear();
                            var ex = examples[selected];

                            drawLattice(ex.subgroups, ex.subEdges, 10, 'Subgroup Lattice', viz.colors.blue);
                            drawLattice(ex.fields, ex.fieldEdges, viz.width * 0.52, 'Field Lattice', viz.colors.orange);

                            ctx.strokeStyle = viz.colors.purple;
                            ctx.lineWidth = 1;
                            ctx.setLineDash([4, 4]);
                            var halfW = viz.width * 0.42;
                            var topY = 50;
                            var botY = viz.height - 30;
                            for (var i = 0; i < ex.subgroups.length; i++) {
                                var sg = ex.subgroups[i];
                                var fd = ex.fields[i];
                                var sx = 10 + sg.x * halfW;
                                var sy = topY + sg.y * (botY - topY);
                                var fx = viz.width * 0.52 + fd.x * halfW;
                                var fy = topY + fd.y * (botY - topY);
                                ctx.beginPath();
                                ctx.moveTo(sx + 8, sy);
                                ctx.lineTo(fx - 8, fy);
                                ctx.stroke();
                            }
                            ctx.setLineDash([]);

                            ctx.fillStyle = viz.colors.purple;
                            ctx.font = '12px -apple-system,sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('\\u2194 Galois correspondence (order-reversing)', viz.width / 2, viz.height - 8);
                        }

                        draw();
                        controls.querySelectorAll('.viz-btn').forEach(function(btn) {
                            btn.addEventListener('click', function() {
                                selected = parseInt(this.getAttribute('data-idx'));
                                draw();
                            });
                        });
                    }
                },
                {
                    id: 'x4-minus-2-lattice',
                    setup: function(container, controls) {
                        var viz = new VizEngine(container, { scale: 40 });
                        var ctx = viz.ctx;

                        var subgroups = [
                            { label: 'D\\u2084', y: 0.05, order: 8 },
                            { label: '<r>', y: 0.25, order: 4 },
                            { label: '<r\\u00b2,s>', y: 0.25, order: 4 },
                            { label: '<r\\u00b2,rs>', y: 0.25, order: 4 },
                            { label: '<r\\u00b2>', y: 0.5, order: 2 },
                            { label: '<s>', y: 0.5, order: 2 },
                            { label: '<rs>', y: 0.5, order: 2 },
                            { label: '<r\\u00b2s>', y: 0.5, order: 2 },
                            { label: '<r\\u00b3s>', y: 0.5, order: 2 },
                            { label: '{e}', y: 0.8, order: 1 }
                        ];

                        var fields = [
                            { label: 'Q', y: 0.05 },
                            { label: 'Q(i)', y: 0.25 },
                            { label: 'Q(\\u221a2)', y: 0.25 },
                            { label: 'Q(\\u221a(-2))', y: 0.25 },
                            { label: 'Q(i,\\u221a2)', y: 0.5 },
                            { label: 'Q(\\u2074\\u221a2)', y: 0.5 },
                            { label: 'Q(i\\u2074\\u221a2)', y: 0.5 },
                            { label: 'Q((1+i)\\u2074\\u221a2)', y: 0.5 },
                            { label: 'Q((1-i)\\u2074\\u221a2)', y: 0.5 },
                            { label: 'Q(\\u2074\\u221a2,i)', y: 0.8 }
                        ];

                        var showSide = 'both';

                        controls.innerHTML = '<div style="display:flex;gap:8px;justify-content:center;">' +
                            '<button class="viz-btn" data-side="subgroups">Subgroups of D\\u2084</button>' +
                            '<button class="viz-btn" data-side="fields">Intermediate Fields</button>' +
                            '<button class="viz-btn" data-side="both">Both (Correspondence)</button>' +
                            '</div>';

                        function draw() {
                            viz.clear();
                            ctx.fillStyle = viz.colors.white;
                            ctx.font = 'bold 16px -apple-system,sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('Galois Correspondence for x\\u2074 - 2 over Q', viz.width / 2, 28);
                            ctx.font = '12px -apple-system,sans-serif';
                            ctx.fillStyle = viz.colors.text;
                            ctx.fillText('Gal(Q(\\u2074\\u221a2, i)/Q) \\u2245 D\\u2084  (dihedral of order 8)', viz.width / 2, 48);

                            var topY = 70, botY = viz.height - 20;
                            var margin = 30;
                            var w = viz.width - 2 * margin;

                            function getXPositions(count, yLevel) {
                                var positions = [];
                                if (count === 1) return [w / 2 + margin];
                                for (var i = 0; i < count; i++) {
                                    positions.push(margin + (i + 0.5) * w / count);
                                }
                                return positions;
                            }

                            function drawLayer(items, color, side) {
                                var layers = {};
                                for (var i = 0; i < items.length; i++) {
                                    var yKey = items[i].y.toFixed(2);
                                    if (!layers[yKey]) layers[yKey] = [];
                                    layers[yKey].push({ idx: i, item: items[i] });
                                }

                                var positions = [];
                                for (var key in layers) {
                                    var layer = layers[key];
                                    var yVal = parseFloat(key);
                                    var screenY = topY + yVal * (botY - topY);
                                    var xs = getXPositions(layer.length, yVal);
                                    for (var j = 0; j < layer.length; j++) {
                                        positions[layer[j].idx] = { x: xs[j], y: screenY };
                                    }
                                }

                                var edges = [
                                    [0,1],[0,2],[0,3],
                                    [1,4],[1,5],[1,6],
                                    [2,4],[2,5],[2,7],[2,8],
                                    [3,4],[3,6],[3,7],[3,8],
                                    [4,9],[5,9],[6,9],[7,9],[8,9]
                                ];

                                ctx.strokeStyle = 'rgba(255,255,255,0.15)';
                                ctx.lineWidth = 1;
                                for (var e = 0; e < edges.length; e++) {
                                    var a = positions[edges[e][0]];
                                    var b = positions[edges[e][1]];
                                    if (a && b) {
                                        ctx.beginPath();
                                        ctx.moveTo(a.x, a.y);
                                        ctx.lineTo(b.x, b.y);
                                        ctx.stroke();
                                    }
                                }

                                for (var k = 0; k < items.length; k++) {
                                    if (!positions[k]) continue;
                                    ctx.fillStyle = color;
                                    ctx.beginPath();
                                    ctx.arc(positions[k].x, positions[k].y, 5, 0, 2 * Math.PI);
                                    ctx.fill();

                                    ctx.fillStyle = viz.colors.white;
                                    ctx.font = '10px -apple-system,sans-serif';
                                    ctx.textAlign = 'center';
                                    ctx.textBaseline = 'bottom';
                                    ctx.fillText(items[k].label, positions[k].x, positions[k].y - 8);
                                }
                            }

                            if (showSide === 'subgroups' || showSide === 'both') {
                                drawLayer(subgroups, viz.colors.blue, 'left');
                            }
                            if (showSide === 'fields') {
                                drawLayer(fields, viz.colors.orange, 'right');
                            }
                            if (showSide === 'both') {
                                ctx.fillStyle = viz.colors.text;
                                ctx.font = '11px -apple-system,sans-serif';
                                ctx.textAlign = 'center';
                                ctx.fillText('(Subgroups shown; fields are the order-reversed mirror image)', viz.width / 2, viz.height - 5);
                            }
                        }

                        draw();
                        controls.querySelectorAll('.viz-btn').forEach(function(btn) {
                            btn.addEventListener('click', function() {
                                showSide = this.getAttribute('data-side');
                                draw();
                            });
                        });
                    }
                }
            ],
            exercises: [
                {
                    id: 'ch16-ex11',
                    type: 'short-answer',
                    question: 'Let K/Q be a Galois extension with Gal(K/Q) isomorphic to Z/6Z. How many intermediate fields are there (including Q and K)? List their degrees over Q.',
                    hint: 'Count the subgroups of Z/6Z.',
                    solution: 'Z/6Z is cyclic of order 6. Its subgroups are of order 1, 2, 3, and 6 (one for each divisor of 6). By the fundamental theorem, there are 4 intermediate fields: Q (degree 1), the fixed field of the order-3 subgroup (degree 6/3 = 2), the fixed field of the order-2 subgroup (degree 6/2 = 3), and K (degree 6).'
                },
                {
                    id: 'ch16-ex12',
                    type: 'short-answer',
                    question: 'In the Galois correspondence for S_3 acting on the splitting field of x^3 - 2, which intermediate fields are Galois over Q? Why?',
                    hint: 'Which subgroups of S_3 are normal?',
                    solution: 'By the fundamental theorem, an intermediate field E is Galois over Q if and only if the corresponding subgroup H is normal in S_3. The normal subgroups of S_3 are {e}, A_3, and S_3 itself. So the Galois intermediate fields are: Q (corresponding to S_3), Q(omega) (corresponding to A_3, with Gal(Q(omega)/Q) = S_3/A_3 = Z/2Z), and K (corresponding to {e}).'
                },
                {
                    id: 'ch16-ex13',
                    type: 'short-answer',
                    question: 'Let K/F be Galois with Gal(K/F) isomorphic to S_4. How many intermediate fields E with [E:F] = 2 are there?',
                    hint: 'Intermediate fields of degree 2 over F correspond to subgroups H of index 2 in S_4, i.e., |H| = 12.',
                    solution: '[E:F] = 2 corresponds to [G:H] = 2, meaning |H| = |S_4|/2 = 12. A subgroup of index 2 is always normal. The only subgroup of S_4 of order 12 is A_4 (the alternating group). So there is exactly 1 intermediate field of degree 2 over F.'
                },
                {
                    id: 'ch16-ex14',
                    type: 'short-answer',
                    question: 'Prove that if K/F is Galois with abelian Galois group, then every intermediate field E is Galois over F.',
                    hint: 'Use the normality criterion from the fundamental theorem and the fact that every subgroup of an abelian group is normal.',
                    solution: 'Let G = Gal(K/F), which is abelian. Let E be an intermediate field, and let H = Gal(K/E) be the corresponding subgroup. Since G is abelian, every subgroup is normal, so H is normal in G. By the normality criterion in the fundamental theorem, E = K^H is Galois over F, with Gal(E/F) = G/H.'
                }
            ]
        },

        // ===== SECTION 5: Computing Galois Groups =====
        {
            id: 'ch16-sec05',
            title: 'Computing Galois Groups',
            content: `
<h2>5 &middot; Computing Galois Groups</h2>

<div class="env-block intuition">
<div class="env-label">From Theory to Practice</div>
The previous sections developed the abstract machinery of Galois theory. In practice, one often starts with a polynomial and wants to determine its Galois group. This section develops concrete computational tools: the discriminant, which detects whether the Galois group lands inside the alternating group; resolvent polynomials, which further pin down the group; and direct techniques for low-degree polynomials.
</div>

<h3>5.1 &middot; Galois Group of a Polynomial</h3>

<div class="env-block definition">
<div class="env-label">Definition &mdash; Galois Group of a Polynomial</div>
Let \\(f \\in F[x]\\) be a separable polynomial with roots \\(\\alpha_1, \\ldots, \\alpha_n\\) in its splitting field \\(K\\). The <strong>Galois group of \\(f\\)</strong> is \\(\\operatorname{Gal}(f) = \\operatorname{Gal}(K/F)\\). Since each automorphism permutes the roots, we have a natural embedding
\\[\\operatorname{Gal}(f) \\hookrightarrow S_n\\]
where \\(n = \\deg f\\). Thus the Galois group is (isomorphic to) a transitive subgroup of \\(S_n\\) (transitive because \\(f\\) is irreducible if and only if the action is transitive).
</div>

<div class="env-block proposition">
<div class="env-label">Proposition &mdash; Irreducibility and Transitivity</div>
If \\(f \\in F[x]\\) is irreducible of degree \\(n\\), then \\(\\operatorname{Gal}(f)\\) acts transitively on the roots of \\(f\\). In particular, \\(n \\mid |\\operatorname{Gal}(f)|\\).
</div>

<h3>5.2 &middot; The Discriminant</h3>

<div class="env-block definition">
<div class="env-label">Definition &mdash; Discriminant</div>
Let \\(f \\in F[x]\\) have roots \\(\\alpha_1, \\ldots, \\alpha_n\\). The <strong>discriminant</strong> of \\(f\\) is
\\[\\Delta(f) = \\prod_{i < j} (\\alpha_i - \\alpha_j)^2.\\]
Equivalently, if \\(\\delta = \\prod_{i < j}(\\alpha_i - \\alpha_j)\\), then \\(\\Delta = \\delta^2\\). Note that \\(\\Delta \\in F\\) (it is a symmetric function of the roots).
</div>

<div class="env-block theorem">
<div class="env-label">Theorem &mdash; Discriminant and the Alternating Group</div>
Let \\(f \\in F[x]\\) be separable with Galois group \\(G \\le S_n\\). Then:
<ul>
<li>\\(G \\le A_n\\) if and only if \\(\\Delta(f)\\) is a perfect square in \\(F\\) (i.e., \\(\\delta \\in F\\)).</li>
<li>\\(G \\not\\le A_n\\) if and only if \\(\\delta \\notin F\\) (equivalently, \\(\\sqrt{\\Delta} \\notin F\\)).</li>
</ul>
</div>

<div class="env-block proof">
<div class="env-label">Proof Idea</div>
The element \\(\\delta = \\prod_{i<j}(\\alpha_i - \\alpha_j)\\) lies in the splitting field \\(K\\). An automorphism \\(\\sigma\\) permuting the roots acts on \\(\\delta\\) by \\(\\sigma(\\delta) = \\operatorname{sgn}(\\sigma) \\cdot \\delta\\). Thus \\(\\delta\\) is fixed by \\(G\\) if and only if every element of \\(G\\) is an even permutation, i.e., \\(G \\le A_n\\). Since \\(K^G = F\\) for a Galois extension, \\(\\delta \\in F\\) if and only if \\(G \\le A_n\\).
</div>

<div class="env-block example">
<div class="env-label">Example &mdash; Discriminant of a Quadratic</div>
For \\(f(x) = x^2 + bx + c\\) with roots \\(\\alpha_1, \\alpha_2\\), the discriminant is
\\[\\Delta = (\\alpha_1 - \\alpha_2)^2 = b^2 - 4c.\\]
If \\(\\Delta\\) is a perfect square in \\(F\\), then \\(f\\) splits in \\(F\\) and \\(\\operatorname{Gal}(f)\\) is trivial. Otherwise, \\(\\operatorname{Gal}(f) \\cong \\mathbb{Z}/2\\mathbb{Z}\\).
</div>

<div class="env-block example">
<div class="env-label">Example &mdash; Discriminant of a Cubic</div>
For \\(f(x) = x^3 + px + q\\) (depressed cubic), the discriminant is
\\[\\Delta = -4p^3 - 27q^2.\\]
If \\(f\\) is irreducible over \\(\\mathbb{Q}\\):
<ul>
<li>\\(\\Delta > 0\\) (and a perfect square in \\(\\mathbb{Q}\\)): \\(\\operatorname{Gal}(f) \\cong A_3 \\cong \\mathbb{Z}/3\\mathbb{Z}\\).</li>
<li>\\(\\Delta\\) is not a perfect square: \\(\\operatorname{Gal}(f) \\cong S_3\\).</li>
</ul>
</div>

<h3>5.3 &middot; Techniques for Low-Degree Polynomials</h3>

<div class="env-block theorem">
<div class="env-label">Determining the Galois Group of an Irreducible Cubic</div>
Let \\(f \\in \\mathbb{Q}[x]\\) be an irreducible cubic. Then:
<ul>
<li>If \\(\\Delta(f)\\) is a perfect square in \\(\\mathbb{Q}\\), then \\(\\operatorname{Gal}(f) \\cong A_3 \\cong \\mathbb{Z}/3\\mathbb{Z}\\).</li>
<li>Otherwise, \\(\\operatorname{Gal}(f) \\cong S_3\\).</li>
</ul>
(These are the only transitive subgroups of \\(S_3\\).)
</div>

<div class="env-block example">
<div class="env-label">Example &mdash; \\(f(x) = x^3 - 3x + 1\\)</div>
The discriminant is \\(\\Delta = -4(-3)^3 - 27(1)^2 = 108 - 27 = 81 = 9^2\\). Since \\(81\\) is a perfect square and \\(f\\) is irreducible over \\(\\mathbb{Q}\\) (no rational roots by the rational root theorem), we conclude \\(\\operatorname{Gal}(f) \\cong A_3 \\cong \\mathbb{Z}/3\\mathbb{Z}\\).
</div>

<div class="env-block example">
<div class="env-label">Example &mdash; \\(f(x) = x^3 - 2\\)</div>
The discriminant is \\(\\Delta = -4(0)^3 - 27(-2)^2 = -108\\). Since \\(-108\\) is not a perfect square in \\(\\mathbb{Q}\\), we conclude \\(\\operatorname{Gal}(f) \\cong S_3\\).
</div>

<h3>5.4 &middot; Resolvent Polynomials and Quartics</h3>

<div class="env-block definition">
<div class="env-label">Definition &mdash; Resolvent Cubic</div>
For a quartic \\(f(x) = x^4 + bx^2 + cx + d\\) with roots \\(\\alpha_1, \\alpha_2, \\alpha_3, \\alpha_4\\), define
\\[\\theta_1 = \\alpha_1\\alpha_2 + \\alpha_3\\alpha_4, \\quad \\theta_2 = \\alpha_1\\alpha_3 + \\alpha_2\\alpha_4, \\quad \\theta_3 = \\alpha_1\\alpha_4 + \\alpha_2\\alpha_3.\\]
The <strong>resolvent cubic</strong> is the polynomial whose roots are \\(\\theta_1, \\theta_2, \\theta_3\\):
\\[g(x) = (x - \\theta_1)(x - \\theta_2)(x - \\theta_3).\\]
The coefficients of \\(g\\) lie in \\(F\\) (they are symmetric functions of the \\(\\alpha_i\\)).
</div>

<div class="env-block theorem">
<div class="env-label">Theorem &mdash; Galois Group of an Irreducible Quartic</div>
Let \\(f \\in \\mathbb{Q}[x]\\) be an irreducible quartic with resolvent cubic \\(g\\). The Galois group \\(G = \\operatorname{Gal}(f)\\) is determined as follows:
<ol>
<li>If \\(g\\) is irreducible over \\(\\mathbb{Q}\\):
  <ul>
  <li>\\(\\Delta(f)\\) is a square in \\(\\mathbb{Q}\\) \\(\\Rightarrow G \\cong A_4\\).</li>
  <li>\\(\\Delta(f)\\) is not a square \\(\\Rightarrow G \\cong S_4\\).</li>
  </ul>
</li>
<li>If \\(g\\) has exactly one rational root:
  <ul>
  <li>\\(G \\cong D_4\\) (dihedral group of order 8).</li>
  </ul>
</li>
<li>If \\(g\\) splits completely over \\(\\mathbb{Q}\\):
  <ul>
  <li>\\(\\Delta(f)\\) is a square \\(\\Rightarrow G \\cong V_4\\) (Klein four-group).</li>
  <li>\\(\\Delta(f)\\) is not a square \\(\\Rightarrow G \\cong \\mathbb{Z}/4\\mathbb{Z}\\).</li>
  </ul>
</li>
</ol>
(The five groups \\(S_4, A_4, D_4, V_4, \\mathbb{Z}/4\\mathbb{Z}\\) are exactly the transitive subgroups of \\(S_4\\), up to conjugacy.)
</div>

<div class="env-block example">
<div class="env-label">Example &mdash; \\(f(x) = x^4 - 2\\)</div>
<p>
The splitting field is \\(\\mathbb{Q}(\\sqrt[4]{2}, i)\\), which has degree 8 over \\(\\mathbb{Q}\\). The resolvent cubic turns out to be \\(g(x) = x^3 + 8\\), which has one rational root (\\(x = -2\\)) and two complex roots. By the algorithm above, \\(G \\cong D_4\\).
</p>
</div>

<div class="env-block example">
<div class="env-label">Example &mdash; \\(f(x) = x^4 + 1\\)</div>
<p>
Over \\(\\mathbb{Q}\\), this polynomial is irreducible (by Eisenstein at \\(p=2\\) after substituting \\(x \\mapsto x+1\\)). Its roots are the primitive 8th roots of unity. The splitting field is \\(\\mathbb{Q}(\\zeta_8) = \\mathbb{Q}(\\sqrt{2}, i)\\), which has degree 4 over \\(\\mathbb{Q}\\). The Galois group is \\(\\operatorname{Gal}(\\mathbb{Q}(\\zeta_8)/\\mathbb{Q}) \\cong (\\mathbb{Z}/8\\mathbb{Z})^\\times \\cong V_4\\).
</p>
</div>

<h3>5.5 &middot; Summary of Computational Strategy</h3>

<div class="env-block intuition">
<div class="env-label">Algorithm for Computing Galois Groups</div>
Given an irreducible polynomial \\(f \\in \\mathbb{Q}[x]\\) of degree \\(n\\):
<ol>
<li><strong>Degree 2:</strong> \\(\\operatorname{Gal}(f) \\cong \\mathbb{Z}/2\\mathbb{Z}\\) (always, since \\(f\\) is irreducible).</li>
<li><strong>Degree 3:</strong> Compute \\(\\Delta(f)\\). If \\(\\Delta\\) is a perfect square, \\(\\operatorname{Gal}(f) \\cong A_3\\); otherwise \\(\\operatorname{Gal}(f) \\cong S_3\\).</li>
<li><strong>Degree 4:</strong> Compute the resolvent cubic \\(g\\) and \\(\\Delta(f)\\). Use the table above.</li>
<li><strong>Degree 5+:</strong> The situation becomes more complex. Tools include resolvent polynomials, cycle types (using reduction modulo primes), and computational algebra systems.</li>
</ol>
</div>

<div class="viz-container" data-viz="galois-group-calculator">
<div class="viz-canvas" id="galois-group-calculator"></div>
<div class="viz-controls" id="galois-group-calculator-controls"></div>
<div class="viz-caption">Compute the Galois group of irreducible cubics \\(x^3 + px + q\\) over \\(\\mathbb{Q}\\). Adjust \\(p\\) and \\(q\\) to see how the discriminant determines the Galois group.</div>
</div>

<div class="viz-container" data-viz="discriminant-landscape">
<div class="viz-canvas" id="discriminant-landscape"></div>
<div class="viz-controls" id="discriminant-landscape-controls"></div>
<div class="viz-caption">The discriminant landscape \\(\\Delta = -4p^3 - 27q^2\\) for depressed cubics \\(x^3 + px + q\\). The curve \\(\\Delta = 0\\) separates regions where the cubic has distinct real roots from regions with one real and two complex roots.</div>
</div>
`,
            visualizations: [
                {
                    id: 'galois-group-calculator',
                    setup: function(container, controls) {
                        var viz = new VizEngine(container, { scale: 40 });
                        var ctx = viz.ctx;

                        var p = -3, q = 1;

                        controls.innerHTML = '<div style="display:flex;gap:16px;flex-wrap:wrap;justify-content:center;align-items:center;">' +
                            '<label style="color:#8b949e;">p: <input type="range" id="galcalc-p" min="-10" max="10" step="1" value="-3"> <span id="galcalc-p-val">-3</span></label>' +
                            '<label style="color:#8b949e;">q: <input type="range" id="galcalc-q" min="-10" max="10" step="1" value="1"> <span id="galcalc-q-val">1</span></label>' +
                            '</div>';

                        var pSlider = controls.querySelector('#galcalc-p');
                        var qSlider = controls.querySelector('#galcalc-q');
                        var pVal = controls.querySelector('#galcalc-p-val');
                        var qVal = controls.querySelector('#galcalc-q-val');

                        function isIrreducible(p, q) {
                            for (var r = -20; r <= 20; r++) {
                                if (r * r * r + p * r + q === 0) return false;
                            }
                            return true;
                        }

                        function isPerfectSquare(n) {
                            if (n < 0) return false;
                            var s = Math.round(Math.sqrt(n));
                            return s * s === n;
                        }

                        function draw() {
                            viz.clear();

                            var disc = -4 * p * p * p - 27 * q * q;
                            var irred = isIrreducible(p, q);

                            ctx.fillStyle = viz.colors.white;
                            ctx.font = 'bold 18px -apple-system,sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('f(x) = x\u00b3 + (' + p + ')x + (' + q + ')', viz.width / 2, 40);

                            ctx.font = '14px -apple-system,sans-serif';
                            ctx.fillStyle = viz.colors.blue;
                            ctx.fillText('Discriminant: \\u0394 = -4(' + p + ')\\u00b3 - 27(' + q + ')\\u00b2 = ' + disc, viz.width / 2, 70);

                            ctx.fillStyle = irred ? viz.colors.green : viz.colors.red;
                            ctx.fillText('Irreducible over Q: ' + (irred ? 'Yes' : 'No (has rational root)'), viz.width / 2, 95);

                            if (irred) {
                                var sqr = isPerfectSquare(disc);
                                ctx.fillStyle = viz.colors.orange;
                                ctx.fillText('\\u0394 is a perfect square: ' + (sqr ? 'Yes' : 'No'), viz.width / 2, 120);

                                var galGroup = sqr ? 'A\\u2083 \\u2245 Z/3Z (cyclic, order 3)' : 'S\\u2083 (symmetric, order 6)';
                                ctx.fillStyle = viz.colors.teal;
                                ctx.font = 'bold 16px -apple-system,sans-serif';
                                ctx.fillText('Gal(f) \\u2245 ' + galGroup, viz.width / 2, 155);
                            } else {
                                ctx.fillStyle = viz.colors.text;
                                ctx.fillText('(Galois group analysis requires irreducibility)', viz.width / 2, 120);
                            }

                            var graphY0 = 190;
                            var graphH = viz.height - graphY0 - 20;
                            var graphW = viz.width - 80;
                            var gLeft = 40;
                            var gScale = graphH / 20;
                            var xScale = graphW / 10;

                            ctx.strokeStyle = viz.colors.grid;
                            ctx.lineWidth = 0.5;
                            ctx.beginPath();
                            ctx.moveTo(gLeft, graphY0 + graphH / 2);
                            ctx.lineTo(gLeft + graphW, graphY0 + graphH / 2);
                            ctx.stroke();
                            ctx.beginPath();
                            ctx.moveTo(gLeft + graphW / 2, graphY0);
                            ctx.lineTo(gLeft + graphW / 2, graphY0 + graphH);
                            ctx.stroke();

                            ctx.strokeStyle = viz.colors.blue;
                            ctx.lineWidth = 2;
                            ctx.beginPath();
                            var first = true;
                            for (var t = -5; t <= 5; t += 0.05) {
                                var fv = t * t * t + p * t + q;
                                var sx = gLeft + graphW / 2 + t * xScale;
                                var sy = graphY0 + graphH / 2 - fv * gScale;
                                sy = Math.max(graphY0, Math.min(graphY0 + graphH, sy));
                                if (first) { ctx.moveTo(sx, sy); first = false; }
                                else ctx.lineTo(sx, sy);
                            }
                            ctx.stroke();

                            ctx.fillStyle = viz.colors.text;
                            ctx.font = '11px -apple-system,sans-serif';
                            ctx.textAlign = 'center';
                            ctx.fillText('Graph of f(x)', viz.width / 2, graphY0 + graphH + 15);

                            var realRoots = [];
                            for (var r = -500; r <= 500; r++) {
                                var rv = r / 100;
                                var fv1 = rv * rv * rv + p * rv + q;
                                var rv2 = (r + 1) / 100;
                                var fv2 = rv2 * rv2 * rv2 + p * rv2 + q;
                                if (fv1 * fv2 <= 0) {
                                    realRoots.push((rv + rv2) / 2);
                                }
                            }

                            for (var ri = 0; ri < realRoots.length; ri++) {
                                var rx = gLeft + graphW / 2 + realRoots[ri] * xScale;
                                var ry = graphY0 + graphH / 2;
                                ctx.fillStyle = viz.colors.orange;
                                ctx.beginPath();
                                ctx.arc(rx, ry, 5, 0, 2 * Math.PI);
                                ctx.fill();
                            }

                            ctx.fillStyle = viz.colors.text;
                            ctx.font = '11px -apple-system,sans-serif';
                            ctx.fillText('Real roots: ' + realRoots.length + (realRoots.length < 3 ? ' real + ' + (3 - realRoots.length) + ' complex' : ' (all real)'), viz.width / 2, graphY0 - 5);
                        }

                        draw();

                        pSlider.addEventListener('input', function() {
                            p = parseInt(this.value);
                            pVal.textContent = p;
                            draw();
                        });
                        qSlider.addEventListener('input', function() {
                            q = parseInt(this.value);
                            qVal.textContent = q;
                            draw();
                        });
                    }
                },
                {
                    id: 'discriminant-landscape',
                    setup: function(container, controls) {
                        var viz = new VizEngine(container, { scale: 30, originX: 280, originY: 200 });
                        var ctx = viz.ctx;

                        function draw() {
                            viz.clear();

                            var w = viz.width, h = viz.height;
                            var ox = viz.originX, oy = viz.originY;
                            var sc = viz.scale;

                            var imgData = ctx.createImageData(w, h);
                            for (var py = 0; py < h; py++) {
                                for (var px = 0; px < w; px++) {
                                    var pCoord = (px - ox) / sc;
                                    var qCoord = -(py - oy) / sc;
                                    var disc = -4 * pCoord * pCoord * pCoord - 27 * qCoord * qCoord;

                                    var idx = (py * w + px) * 4;
                                    if (Math.abs(disc) < 0.5 * sc) {
                                        imgData.data[idx] = 255;
                                        imgData.data[idx + 1] = 255;
                                        imgData.data[idx + 2] = 255;
                                        imgData.data[idx + 3] = 255;
                                    } else if (disc > 0) {
                                        var intensity = Math.min(255, 40 + Math.sqrt(disc) * 8);
                                        imgData.data[idx] = 30;
                                        imgData.data[idx + 1] = Math.floor(intensity * 0.7);
                                        imgData.data[idx + 2] = Math.floor(intensity);
                                        imgData.data[idx + 3] = 255;
                                    } else {
                                        var intensity2 = Math.min(255, 40 + Math.sqrt(-disc) * 8);
                                        imgData.data[idx] = Math.floor(intensity2 * 0.9);
                                        imgData.data[idx + 1] = Math.floor(intensity2 * 0.3);
                                        imgData.data[idx + 2] = 30;
                                        imgData.data[idx + 3] = 255;
                                    }
                                }
                            }
                            ctx.putImageData(imgData, 0, 0);

                            ctx.strokeStyle = 'rgba(255,255,255,0.4)';
                            ctx.lineWidth = 1;
                            ctx.beginPath();
                            ctx.moveTo(0, oy);
                            ctx.lineTo(w, oy);
                            ctx.stroke();
                            ctx.beginPath();
                            ctx.moveTo(ox, 0);
                            ctx.lineTo(ox, h);
                            ctx.stroke();

                            ctx.fillStyle = 'rgba(255,255,255,0.8)';
                            ctx.font = '12px -apple-system,sans-serif';
                            ctx.textAlign = 'left';
                            ctx.fillText('p', w - 20, oy - 8);
                            ctx.fillText('q', ox + 8, 15);

                            ctx.font = 'bold 13px -apple-system,sans-serif';
                            ctx.fillStyle = 'rgba(88,166,255,0.9)';
                            ctx.fillText('\\u0394 > 0: three distinct real roots', 10, 20);
                            ctx.fillStyle = 'rgba(240,136,62,0.9)';
                            ctx.fillText('\\u0394 < 0: one real, two complex roots', 10, 38);
                            ctx.fillStyle = 'rgba(255,255,255,0.9)';
                            ctx.fillText('\\u0394 = 0: repeated root', 10, 56);

                            ctx.fillStyle = viz.colors.green;
                            ctx.font = '11px -apple-system,sans-serif';
                            ctx.fillText('Curve: -4p\\u00b3 - 27q\\u00b2 = 0', 10, h - 10);
                        }

                        draw();
                    }
                }
            ],
            exercises: [
                {
                    id: 'ch16-ex15',
                    type: 'short-answer',
                    question: 'Compute the discriminant of f(x) = x^3 - 7x + 6. Is f irreducible? If so, determine Gal(f).',
                    hint: 'First check for rational roots using the rational root theorem.',
                    solution: 'Trying rational roots: f(1) = 1 - 7 + 6 = 0, so x = 1 is a root. Thus f is NOT irreducible: f(x) = (x-1)(x^2+x-6) = (x-1)(x-2)(x+3). Since f splits completely over Q, Gal(f) is trivial.'
                },
                {
                    id: 'ch16-ex16',
                    type: 'short-answer',
                    question: 'Determine the Galois group of f(x) = x^3 + 3x + 1 over Q.',
                    hint: 'Check irreducibility (rational root theorem), then compute the discriminant.',
                    solution: 'By the rational root theorem, possible rational roots are +/-1. f(1) = 5 and f(-1) = -3, so f has no rational roots and is irreducible (degree 3). The discriminant is Delta = -4(3)^3 - 27(1)^2 = -108 - 27 = -135. Since -135 is not a perfect square (it is negative), Gal(f) is isomorphic to S_3.'
                },
                {
                    id: 'ch16-ex17',
                    type: 'short-answer',
                    question: 'Let f(x) = x^3 - 3x - 1. Show that Gal(f) is isomorphic to Z/3Z.',
                    hint: 'Compute the discriminant and check if it is a perfect square.',
                    solution: 'First, f has no rational roots (check +/-1). So f is irreducible over Q. The discriminant is Delta = -4(-3)^3 - 27(-1)^2 = 108 - 27 = 81 = 9^2. Since Delta is a perfect square, Gal(f) is isomorphic to A_3 = Z/3Z.'
                },
                {
                    id: 'ch16-ex18',
                    type: 'short-answer',
                    question: 'Explain why the Galois group of an irreducible polynomial of prime degree p over Q always contains a p-cycle (when viewed as a subgroup of S_p).',
                    hint: 'Use the fact that the Galois group acts transitively on the roots, and Cauchy\'s theorem.',
                    solution: 'Since f is irreducible of degree p, the Galois group G acts transitively on the p roots, so p divides |G| (orbit-stabilizer). By Cauchy\'s theorem, G contains an element of order p. In S_p, the only elements of order p are p-cycles (since a permutation of {1,...,p} with order p must be a single p-cycle). So G contains a p-cycle.'
                }
            ]
        }
    ]
});
